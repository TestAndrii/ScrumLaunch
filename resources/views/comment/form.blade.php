@csrf
<div class="<div class="form-group">
    <label for="my-input">Комментарий:</label>
    <input class="form-control" type="text" name="body" value="{{ $comment->body ?? '' }}" request>
</div>

</div>
